# conformance-audit

Canonical definition: `src/fancy_gpt/catalog/workflows.yaml`.

This is a workflow profile and intentionally does not appear under the Agent Skill bundle. Use `fancy-gpt catalogs workflows` or `fancy-gpt catalogs route ... --workflow conformance-audit` to inspect the resolved sequence/domains/focus/capabilities.
