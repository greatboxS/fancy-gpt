# Workflow profiles

Workflow profiles are orchestration data, not Agent Skills. Each profile composes canonical skills and contributes default domains, focus, report sections, and quality gates. Definitions live in `src/fancy_gpt/catalog/workflows.yaml`.
